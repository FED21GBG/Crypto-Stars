import { Link } from "react-router-dom";
function StartPage() {
    return (
        <section>
            StartPage
            <Link to='/LoginPage'>Login</Link>
        </section>);
}

export default StartPage;